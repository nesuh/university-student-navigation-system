{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "compilerOptions": {
    "outDir": "./dist"  // Ensure the build output goes to dist/
  },
  "include": [
    "src/**/*.ts"  // Ensure only .ts files in src are compiled
  ]
}
